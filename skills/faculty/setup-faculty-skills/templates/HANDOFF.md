<!--
  HANDOFF.md — the teacher-to-teacher briefing. Hard cap: ~2K tokens.
  REWRITTEN by each teacher at session end — a briefing, not a diary; displaced
  content goes to the current unit's .records/handoff-archive.md. Soft context only:
  tone, wobbles, momentum. "What's next" is the ticket frontier's job, not prose.
-->

# Handoff

Rewritten by each teacher at session end. No sessions yet.
